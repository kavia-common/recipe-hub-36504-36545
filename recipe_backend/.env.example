# Backend environment configuration for Recipe Hub (FastAPI)

# The port the backend listens on (informational; configured by process manager)
PORT=3001

# CORS settings: make sure the backend allows the frontend origin
# In FastAPI, configure CORSMiddleware like:
# from fastapi.middleware.cors import CORSMiddleware
# app.add_middleware(
#     CORSMiddleware,
#     allow_origins=["http://localhost:3000"],
#     allow_credentials=True,
#     allow_methods=["*"],
#     allow_headers=["*"],
# )
CORS_ALLOWED_ORIGINS=http://localhost:3000

# JWT and auth related (example placeholders)
JWT_SECRET=replace-with-secure-secret
JWT_ALGORITHM=HS256
JWT_EXPIRES_IN_MIN=60
